# ZEIT OAuth2 client example

How to run the app.

- get OAuath client id and client secret (contact us)
- run `npm install`
- start the server like `CLIENT_ID=xxx CLIENT_SECRET=xxx PORT=4000 npm start`
- access the top page.
